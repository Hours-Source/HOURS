# The planetary radiative layer

**Scope.** The P0 bound, Path C, λ and its Planck ceiling, the drawdown chain and
the solvency gate, responsibility allocation, η from ERA5, capital dual-output —
and the measurement-spine merge that founded the layer.

Migrated from `CLAUDE.md` § Current status on 2026-09-03. Entries are verbatim.

---

## Live state

- **The thermal obligation is a STOCK carried in the ecological domain**, opt-in
  and defaulting to 0.0. It is the one ecological service labour cannot restore
  and is unattributable to any parcel, so it falls OUT of the pristine/current
  partition rather than being wedged into it. See
  [ecological.md § Live state](ecological.md#live-state).
- **λ has a physical ceiling and it is enforced.** `planck_feedback()` derives
  λ_P = 4σT³ = 3.761 W·m⁻²·K⁻¹ from `SIGMA_SB` × `EARTH_EMISSION_TEMPERATURE_K`;
  net feedbacks are amplifying, so λ < λ_Planck is required. **Shipped λ = 1.2 is
  admissible at 0.319 of the bound**, implying 2.561 W·m⁻²·K⁻¹ of net amplifying
  feedback — the quantity a reader can check against the literature. The bound is
  deliberately LOOSE (blackbody 3.761 against the real ≈3.2), erring toward
  admitting too much λ rather than too little. **This is the only physical anchor
  λ has**, and λ is the most leveraged parameter after `delta_T_lo`.
- **The layer withholds the budget where the sign is undetermined.** Carrying λ
  honestly widens the indeterminate band 2×, and that is reported rather than
  resolved.
- **`epsilon_current` is derived where an inventory exists**; global ε_max keeps a
  chosen `epsilon_current` and travels with its sensitivity band, because no
  measured world capital inventory in TEH exists.

## Open

- **`CDR_LABOR_HOURS_PER_TONNE` (0.6, Tier D) now owns any residual discrepancy
  between the thermal and land layers.** The ecological anchor can no longer
  absorb it — Phase 4f settled that disjunct. *Settles by:* operator staffing data
  beyond the current n=1.
- **The `GUF_ECO_KAPPA_*` constants are engineered-route figures.** Restoration
  implies κ_carbon 0.009–0.048 h/tonne against a shipped 0.6 — a 12–69× gap that
  is a MEASUREMENT of how much cheaper biological replacement is, not an
  inconsistency to reconcile. CONDITIONAL on a placeholder V_s. See
  [ecological.md#phase-3-restoration-cost](ecological.md#phase-3-restoration-cost).
- **λ_equilibrium is not assessable from this data**, and marginal-capacity η was
  built three ways and none is usable. Both are declared limits, not gaps to
  close opportunistically.
- **Capital thermal intensities are Path-D placeholders** — they need IEA/LCA
  data.
- **The capital-profile SCALE is unchecked against any real inventory, and looks
  ~3× out.** Running the US through `epsilon_current_from_inventory()` gives
  ε = 0.777–1.000 (saturated) against `capital_for_epsilon(0.40)` = 4,204
  TEH/capita — the US 2.2–4.7× over, which is not credible at ε≈0.95 while
  158M people work. `mix_spread(0.40)` is only 1.30×, **so the MIX is not the
  problem; the SCALE is** — and the capital→ε loop, the thermal ceiling and the
  corridor all ride on it. *Settles by:* BEA Fixed Assets Table 1.1 (stock) plus
  2.1/3.1ES by asset type — one table over from the flow table already used,
  which gives the inventory but does **not** by itself resolve the 3×. Detail:
  [finding-a-capital-profile-scale](#finding-a-capital-profile-scale).

## Cross-area entries

| Entry | Also | Why |
|---|---|---|
| [measurement-spine-merged](#measurement-spine-merged) | [provenance](provenance.md#history), [personal](personal.md#history) | The merge covers the multiplier and infrastructure spines as well as thermal; filed here because thermal is its largest component |
| [audit-closures](#audit-closures) | [contestability](contestability.md#history), [ecological](ecological.md#history), [provenance](provenance.md#history) | Six gaps across four areas — the corridor contestability axis, thermal ε_current, the four-tag migration, domain balance, the thermal obligation made reachable, and orphan registration |

---

## History

Newest first, verbatim as written. Anchors are stable — link to a specific entry
as `record/thermal.md#<slug>`.

<a id="measurement-spine-merged"></a>

**Measurement spine merged** (`f2a242e`, 2026-08-05, 12 commits): measured inputs replacing chosen constants across the multiplier (O\*NET 30.3/BLS, 751 occupations, 94.2% of employment), infrastructure (currency-free statutory floor from a physical condition census, doctrine-invariant at spread 1.000 vs the monetized path's 10.26×), and thermal (P0 bound, Path C, corridor, capital dual-output, C5 forcing correction, overage/debt reframing, drawdown chain + solvency gate, responsibility allocation over the full 1750–2024 record, η from ERA5 for 258 collectives, derived λ, ε inversion via `capital_for_epsilon`, maintain-vs-replace). Two constants moved from recalled to derived (CO₂ forcing coefficient, λ_historical). What the branch DECLINED to claim is recorded with equal weight: λ_equilibrium is not assessable from this data; marginal-capacity η was built three ways and none is usable; carrying λ honestly widens the indeterminate band 2× and the layer now withholds the budget where the sign is undetermined.

<a id="audit-closures"></a>

**Audit closures** (2026-08-05, branch `fix/audit-close-now`): six gaps found by auditing the framework against its own claims, all closed with no new data.
- **Corridor contestability axis migrated.** `research/corridor.py` was still taking its ceiling from the bare χ = P/K_entry that §8.9 superseded, so the recorded "corridor CLOSED at defaults" finding was produced by a retired invariant. `contestability_ceiling()` now runs the adopted three-channel `exit_financing()` test; the old form survives as `contestability_ceiling_bare_chi()` and `contestability_axes()` reports the disagreement. At defaults the corridor is **OPEN** (nothing binds); under `--bare-chi` it still closes at ε_suff 0.517 vs ceiling 0.290, reproducing the earlier result on demand. `core/dashboard.py` gained `exit_financeable`, which governs the contestability flag when supplied and demotes χ to a YELLOW advisory (χ alone keeps pre-§8.9 behavior).
- **Thermal ε_current derived where an inventory exists.** `thermal_capital.epsilon_current_from_inventory()` + `capital_thermal_ceiling(epsilon_current=None)` default to deriving ε from the same capital that produces Φ, via `civilization_epsilon`. Global ε_max keeps a chosen ε_current — no measured world capital inventory in TEH exists — but `global_ceiling()` now returns `epsilon_max_band`/`binds_within_band` over ε_current ∈ [0.2, 0.6], so the chosen constant travels with its sensitivity. H = B/Φ_auto stays the headline.
- **Provenance four-tag migration finished** for the EOH-generation block, with a retag log. 13 constants now carry epistemic pointers where 6 previously claimed structural status.
- **Domain balance documented** (see below) with regression tests and `arc --domain-shares`.
- **Thermal obligation made reachable**: `scenarios/thermal_load.py` + `scenario run thermal_load`, `--thermal-obligation` on `arc` and `dashboard`.
- **Orphans registered**: `measured_sim`, `multiplier_sensitivity`, `infra_floor`, `thermal_load` in `scenario list`; new `corridor` CLI (`band`, `axes`).

<a id="thermal-sink-vector-build-log"></a>

**THE THERMAL SINK VECTOR — THE FULL BUILD LOG** (2026-08-01 → 2026-08-05, promoted from session memory 2026-09-03). Verbatim as written, newest material appended as the work proceeded, so this entry reads OLDEST-FIRST internally — the one place in `record/` that does. Environment and tooling paragraphs were held back to `notes/README.md`, which is where this repo keeps local machine quirks; nothing else was cut.

---
name: project-thermal-sink-vector
description: Thermal-load (planetary radiative capacity) vector reviewed + P0 provable-ceiling bound built; capital work paused pending it
metadata: 
  node_type: memory
  type: project
  originSessionId: 2437c2f6-8846-4740-9dc4-00f2c310126b
  modified: 2026-08-04T01:58:08.087Z
---
Reviewed `handoffs/Thermal_Sink_EOH_Implementation_Handoff_1_0.md` (2026-08-01, author-flagged as possible major redesign). Verdict: the thermal-load vector is REAL and previously uncounted — no thermal/watt/joule/radiative concept exists in core today (only GUF's TEH-denominated cooling-service κ in the land layer). It's the most physically legitimate critique the project has had: EOH denominates in entropy, and radiative capacity is the one entropy sink labor can't rebuild. Coheres with the multiplier handoff's KNOWN_ISSUES §5 (energy rejected as unit-of-account BECAUSE the shared solar budget/m² is the binding constraint) — two independent handoffs converging.
Errors found in the handoff (flagged, not propagated): (1) essential domains are 7 in repo not 8 — thermal would be the 8th, floor re-derive 7→8 not 8→9; (2) §7.4 says split `ecological_eoh_breakdown`'s `thermal_regulation` category — that category doesn't exist (returns baseline/spike/visible_deferred; the thermal concept lives in GUF/land). Handoff is strong theory but its integration section isn't reconciled to HEAD (same as the infra handoff).
Structural impact: the one genuine SPINE change is §12.1 ε-becomes-a-vector (per-domain ε, scalar retained as EOH-weighted aggregate) — theory-flagged, needs AWol sign-off (same class as price-as-floor / objectivity→transparency). Everything else is additive (new core/thermal.py etc.), ledger identity untouched (E_th = obligation, never TEH). Capital coupling (why the capital work is paused): §12.2 makes infra EOH dual-output (hours+thermal) needing NEW capital-profile fields — net power, embodied energy, physical grid mix, service life — none in CAPITAL_MACHINE_PROFILES today. So the typed-capital adaptation must absorb thermal fields BEFORE picking a schema. Keep multiplier commit ce86619 as-is (independent).
BUILT P0 (author chose "build P0 bound first"; research-tier, advisory-only, NO obligation/TEH): `research/thermal.py` — budget chain E6 residual_thermal_forcing / E7 planetary_budget / E8 allocated_density, E27 iota_floor + eoh_weighted_iota_floor, E29 provable_ceiling_bound → BoundReport, ceiling_bound_sensitivity (§10.2 artifact). Constants in data.py (budget chain = physics/measured; λ/F_GHG/ΔT_lo + ι_floor = CHOSEN placeholders w/ epistemic pointers → measurement ladder D/C/B). Provenance doc + `tests/test_thermal.py` (18). Suite 1862 pass, mypy clean. UNCOMMITTED.
DECISION (2026-08-01, author sign-off, ceremony skipped): demote ε=1 (full automation) from "the target" to ASPIRATIONAL — the target to reach — and elevate a STABLE MEASURABLE CORRIDOR as success in its own right. BUILT SAME DAY (uncommitted, staged): `research/corridor.py` — survival_floor_epsilon (E22, ε_suff computable now), contestability_ceiling (χ crossing), thermal_ceiling (INCONCLUSIVE plug), corridor() → CorridorReport (success = feasible band + met sufficiency floor, NOT ε→1), corridor_stability() (STABLE/NARROWING/BREACHED over horizon). `tests/test_corridor.py` (20). Language demotion applied (signed off): trajectory.py:206 docstring + README core-idea para now frame ε=1 aspirational + corridor-success. Suite 1882 pass, mypy clean (52 files). HONEST corridor findings at reference (pop 1M, 50% workforce×2000h → L_avail=1e9): ε_suff≈0.50–0.53 (must automate ~half just to meet survival); adversarial contestability regime (thin trust) → corridor CLOSED (χ<1 at ε≥0.29 sits BELOW survival floor 0.52 — the two invariants CONFLICT, a real bind the old ε→1 framing hid); well-capitalized regime → OPEN, success=True at ε=0.52 without needing ε=1. Corridor upper bound = unified invariant corridor (tightest of contestability/thermal/…); thermal reads advisory/INCONCLUSIVE until path-C ι.
ORIGINAL DECISION LINE: demote ε=1 from "the target" to ASPIRATIONAL and elevate a STABLE MEASURABLE CORRIDOR as success in its own right. "A collective stable at ε=0.6 with positive corridor + met sufficiency floor is a success, not a failed run at ε=1." This adopts the thermal handoff §1.1/§12.1 positioning as a framework commitment. Theory-flagged → needs a sign-off doc + README/trajectory.py language edits (currently ε framed as "measured progress score toward post-scarcity" in trajectory.py:208 and README ε=0.99=post-scarcity). Corridor readiness split: LOWER bound ε_suff (E22 survival floor) computable NOW from existing inventory (eoh_to_essential_domains + human_eoh_share + total_eoh); UPPER bound ε_max — thermal one INCONCLUSIVE (needs path-C ι), BUT other ceilings already computed (contestability χ<1, fiscal insolvency, ecological collapse). So corridor = [ε_suff, tightest binding ceiling] is definable TODAY; thermal ε_max plugs in when measured ι lands. Reframe is largely unification of what dashboard already checks (conditions I–IV + χ + solvency) + explicit ε_suff + a stability-over-horizon test. Nothing corridor-related exists in core yet. Thermal P0 stays STAGED (uncommitted) per author.
PATH C MEASURED (2026-08-01, built; handoffs/path c - thermal sink reviewed — a complete, honestly-provenanced run): implemented the measured top-down thermal residual, resolving the P0 INCONCLUSIVE bound. `research/thermal_path_c.py` — E1 dissipation_flux (Φ=17.71 TW measured), Eq. C1 measured_epsilon_max (ι and EOH_total CANCEL → NO EOH register needed; the key shortcut), collective utilization U+regime (F11), decarbonization_headroom (F3), global_ceiling. Dataset `reference/data/thermal_path_c.json` (provenance tiers A/B/C/D intact). Constants in data.py (A_LAND_CLAIMED_M2, THERMAL_F_NET_ERF 2.72, THERMAL_F_WMGHG_ERF 3.84, THERMAL_U_FLOOR, THERMAL_EPS_CURRENT — all tagged). Reproduces handoff EXACTLY: Φ=17.71TW, ψ*@3K=2.660, Singapore U=22.39, decarb@3K=+1110TW, ε_max@2.5K=2.58. `tests/test_thermal_path_c.py` (21). Corridor upgraded: `measured_thermal_ceiling(U, ε_current)` — a collective in Contact (U≥1) binds the thermal ceiling at ε_current (F11 flows into the corridor); P0 thermal_ceiling marked SUPERSEDED. Demo: Singapore/S.Korea → corridor CLOSED (thermal binds); US/World → open. Suite 1903 pass, mypy clean (53 files). UNCOMMITTED (staged).
THREE MEASURED FINDINGS (reproduced, provenance-honest): F1 global ceiling does NOT bind at current dissipation (ε_max 2.6–19×) — CONDITIONAL, binds at ~10–50× present Φ (confirms P0). F3 (load-bearing): decarbonization worth ~1000–1100 TW ≈ 60× current dissipation — carbon consumed the budget; handoff recommends P0 be reordered to decarbonization-headroom, not the floor bound. F11 (strongest, now a corridor bound): dense collectives in Contact NOW (Singapore U≈22) while World sits at U≈0.05 → thermal corridor bound is COLLECTIVE-level, global uninformative by ~20×. Provenance uneven: ΔT_lo (Tier D) dominates; national energy (Tier C, unverified) — Singapore robust, Netherlands/S.Korea marginal (30% error flips regime). Path C = 5–10× uncertainty → regime SIGN only, NOT obligation (needs Path B). Re-source: EI workbook (energyinst.org), AR6 Table 7.8, CERES EBAF for η. Handoff's 5 recs implemented: Eq C1 operative, corridor scoped to collectives, F11 primary, F1/F2 conditional, AND F3-first reorder ENACTED (2026-08-01). P0 REORDER: `research/thermal.py` module docstring now leads with F3 decarbonization-headroom as the primary P0 deliverable; added structural `decarbonization_headroom()` (DecarbonizationHeadroom TypedDict, computable from budget-chain constants, gain=alloc(F=0)−alloc(F=measured), ~1110TW@3K/979TW@2K, binds_now flag for the sharp form where budget=0 now but headroom exists); `provable_ceiling_bound` docstring demoted to CONDITIONAL (F1/F2 non-binding globally, confirmed by Path C); ceiling_bound_sensitivity note updated. Path C `decarbonization_headroom` refactored to WRAP the P0 primitive (DRY, `_p0_decarbonization_headroom` import) + adds gain_over_current_dissipation ratio. 3 new F3 tests in test_thermal.py. Suite 1906 pass, mypy clean (53 files). STILL UNCOMMITTED.
HANDOFF 2.0 REVIEWED + C5 APPLIED (2026-08-03, `handoffs/thermal_sink/Thermal_Sink_EOH_Handoff_2_0.md`). Verified every headline against the shipped data (`handoffs/thermal_sink/data/`, gitignored: OWID energy CSV + 31 IGCC-2025a CSVs + `base/` timeseries). CONFIRMED exactly: ERF 3.366 [2.602–4.102]; Φ 551.4 EJ → 17.47 TW from OWID 2024 + κ + nuclear/0.33; ψ 0.129/0.0343; ψ*(3.5)=2.521; determinacy band 2.168/2.805/3.418; ε_max 2.19; the whole Contact list within 0.1–3%; dTXx/dGMST 1.483 (n=76, multi-dataset mean; per-dataset spread 1.33–1.57); marine heatwaves 15.5→62.3, 81.4 in 2024. λ=1.2 and r=0.20 were NEVER STATED in the doc — reverse-engineered from ψ*; now stated in §4.
ERRORS FOUND + FIXED: F1's "binds at 10–50× present dissipation" was stale AND self-contradictory — by Eq. C1 the binding multiple IS ε_max, so it's 2.2× at 3.0 K (≈40 yr at 2%/yr growth), not 10–50×. F3's "~1,100 TW / 60×" was the 2.72-vintage number → 1,374 TW / 78× (total) or 1,267 TW / 72× (anthro). §4.2's upper determinate boundary 3.50 K → 3.42 K (p95/λ), land 5.18 → 5.06 K. §4.1's 93.1% was the EI/TES basis in an OWID-labelled table (OWID basis = 86.7%). EEI 1.19 NOT reproducible from shipped data (0.90–1.07); §6.2's "EEI `total` column is empty" is wrong (populated 1971–2020).
C5 SHIPPED: data.py THERMAL_F_NET_ERF 2.72→3.366 + _P05/_P95 + THERMAL_F_ANTHRO_ERF 3.104 + THERMAL_F_WMGHG_ERF 3.84→3.585 + THERMAL_TXX_PER_GMST 1.48 (all Tier A). `basis="anthro"` added to ForcingBasis; new `determinacy_zone()` makes §4.2 computable (needed a forcing BAND — impossible pre-C5). JSON derived_outputs recomputed + `world_energy_owid_2024` cross-check block + `_superseded` audit trail. Docstrings/tests updated. 1928 tests (was 1921), mypy clean. UNCOMMITTED.
FOUR SIGN-OFF ITEMS (handoff §10, options + recommendations written): §10.1 F3 basis — recommend split (budget on total per C4, gain on anthro since only anthro is removable); default deliberately NOT flipped. §10.2 ε_max ∝ ε_current=0.40 (Tier D) contradicts the "ε is an observable not an input" invariant — recommend reporting the MEASURED ratio H=B/Φ_auto=5.4 as the headline, deriving ε via core/civilization.py. §10.3 ψ* is an EQUILIBRIUM construction while EEI≈1 says we're not there — recommend keeping equilibrium (right frame for commitment accounting) + reporting the pipeline: at 3.0 K, 1.57 K of the 1.77 K apparent headroom is ALREADY COMMITTED. §10.4 λ=1.2 is Tier C and the budget spans 6.5× across λ∈[1.2,1.63]; the shipped data's own energy budget implies λ_hist 1.47–1.54 (runs high vs equilibrium λ — pattern effect, so 1.2 is defensible, but the frame must match the λ). §10.3 and §10.4 are the SAME question.
ERA5 / P2 REWRITTEN: acquisition is 4 files at 00/06/12/18 UTC (not 12 UTC only), 1st of alternate months, every 5th year 1940–2025, FULL single-level catalogue (262 fields, 0.25°). 12 UTC in hand: `rawdata/data12utc.grib`, 176 GB, gitignored (`rawdata/` added to .gitignore). 4×176 GB ≈ 704 GB vs 757 GB free — fits. Old §7 was written for 12-UTC-only and its mitigation 2 (local-solar-time stratification) was NEVER EXECUTABLE — at fixed UTC, local time is perfectly collinear with longitude forever. The 4-hour design REMOVES the diurnal bias (4-point mean unbiased for harmonics 1–3), so η now comes from OLR DIRECTLY (`avg_tnlwrf` W·m⁻², or −`ttr`/3600; clear-sky variant `avg_tnlwrfcs`), not TCWV-as-proxy. Residual = 4th-harmonic aliasing — bound it with a cheap 1-day/24-hour/6-variable pull. Recommend CLEAR-SKY as the allocation basis (all-sky rewards weather; clear-sky measures structural column transparency), all-sky reported alongside. Publish only `reference/data/eta_land.json` + an `examples/` script reading a configurable raw path — never the GRIB.
HONEST P0 FINDING (do NOT overstate; constants not tuned): the floor-based bound (path D) can only OVERSTATE ε_max, so a bound <1 would be conclusive (F2) — but it comes back ε_max ≫ 1 (240–5000×) at every budgeted corner → INCONCLUSIVE. The thermodynamic floor is too low to bind automation. Only "binding" corner is UNBUDGETED (ψ*=0), driven by GHG forcing exhausting the allowance at low λ/ΔT_lo — an F3 statement about decarbonization, not automation intensity. Corridor SIGN is NOT robust to (λ, ΔT_lo) → per §10.2 the extension stays advisory. Conclusion: F2 does not fire from floors; the binding question needs measured ι (path C, top-down national-energy residual). That is the honest next measurement, not P1–P4 build-out. See [[project-multiplier-v5-adoption]] [[project-infrastructure-eoh-floor]].
All of this LANDED on branch `research/measurement-spine` (renamed from research/multiplier-v5-adoption 2026-08-04): 4d8ed4b infrastructure floor, d13613e thermal (P0+PathC+corridor+capital+C5), 605a9be overage+census dual-output. 1959 tests, mypy clean.
OVERAGE REFRAMING BUILT (2026-08-04, 605a9be, author-directed). `research/thermal_overage.py` replaces headroom with debt as the thermal headline: O(ΔT_max) = Φ + (F_total − λ·ΔT_max)·A_earth, signed/unclipped, decomposed into forcing and heat. At ΔT_max=2.0 K: **510 TW overage, of which waste heat is 3.4%** — eliminating EVERY net-additive watt on Earth closes 3.4%. Zeroing needs 1.000 W·m⁻² = 32% of removable anthropogenic forcing. Three properties the F3 gain lacked: (1) no degeneracy — below the opening threshold the gain saturates at (1−r)λΔT·A and the forcing DROPS OUT, so it reports the chosen threshold not the carbon, exactly in the determinate zone; (2) feasibility check — natural forcing 0.262 is a floor labor can't cross, and even at zero anthro forcing commitment is only 0.247 K, so **waste heat alone never exhausts the allowance: the wall is carbon**; (3) the ceiling's cause — at 2.0 K there is NO pre-drawdown ceiling (no budget), post-drawdown ε_max≈20 allocated/25 gross. ε-arc: heat share 0%→8.2% across ε∈[0,0.99], overage moves <15% — automation is not what breaks the budget, at any ε. Equilibrium frame kept deliberately (commitment accounting, not flow) and now printed with every report: committed 2.805 K / delivered 1.23 K / **pipeline 1.58 K**; a transient framing would hide ~964 TW. New constants THERMAL_F_NATURAL_ERF 0.262, THERMAL_GMST_OBSERVED 1.23 (both IGCC 2025a Tier A). CLI `eoh thermal {overage,arc,determinacy,ceiling}` — the thermal layer had NO entry point before.
NEXT (planned, agreed): A2 converter then A3 the SOLVENCY GATE. Thermal overage stays ADVISORY until the gate passes. Gate design: backward query — not "what is ι_drawdown?" but "what would ι_drawdown have to be for this to be survivable?", same epistemic move as F2's floor bound. Inject via the EXISTING hooks `fiscal_snapshot(deferred_ecological=…)` / `ecological_allocation(deferred=…)` — **zero core changes needed**. Gate lives in research/ NOT scenarios/ (scenarios may not import research). A2 uses an explicit tiered chain ΔF → ppm (Myhre) → GtCO₂ → energy (GJ/tCO₂) → EOH rather than one lumped ι. Pass conditions fixed in advance: Trust solvent across arc, ecological/stewardship co-equality preserved, Condition IV holds, full ε-arc coherence, suite+mypy green — ALL FIVE or it stays advisory, and failure is a publishable finding, not something to tune around. Back-of-envelope (Tier C/D, do not quote): 1M-collective share ≈41M EOH ≈68× annual ecological baseline as a one-time stock, ~0.7×/yr over a century — plausibly absorbable, so the gate is worth running properly.
OBLIGATION BOOKED + ERA5 ALIASING MEASURED (2026-08-05, commits 358487b + d7217fe on research/measurement-spine; 2017 tests, mypy clean, tree clean).
AUTHOR DECISIONS: (1) ΔT_max 2.0 K stays default but marked CHOSEN — may be judged too HIGH later and every downward revision ENLARGES the obligation; (2) programme horizon 40 yr = one lifetime of responsibility (CHOSEN, ethical not technical; obligation ∝ 1/horizon); (3) allocation = RESPONSIBILITY (cumulative emissions) not population — "collectives can't burden others based on their choices"; (4) WIRE the obligation into ecological EOH, open to pivot back.
FIRST BOOKED NUMBER: 1M-population collective owes **1,789,175 EOH/yr** (ΔT 2.0 K, 40 yr, 0.6 h/t) vs ecological baseline 714,286 — the domain **more than triples (3.50×)**. GATE PASSES at every ε; Trust gives way at 22.9 h/t = **38× margin**. Binding condition is FISCAL, never labour (tripled domain = 0.2% of labour capacity).
KEY STRUCTURAL RESULT: energy per tonne CANCELS out of the EOH — since ι = energy/labour-per-tonne, **EOH = gross tonnes × labour-hours per tonne**. So CDR_ENERGY_GJ_PER_TONNE affects ONLY the programme's own dissipation, and the whole obligation rests on one Tier D number (CDR_LABOR_HOURS_PER_TONNE = 0.6). CO2_FORCING_COEFFICIENT = 5.645 is Tier A, DERIVED from the shipped IGCC series over 350–426 ppm; self-validates (intercept → 279.8 ppm vs accepted 278); Myhre's 5.35 runs 5.2% low there.
GATE DESIGN LESSON — reusable: two drafts reported FAIL at every ε for reasons unrelated to the load. (a) uncalibrated trust balance 5e8 — the repo's calibrated reference is `scenarios/sweep.epsilon_sweep`'s 3.5e10 + age_ratio 0.30; (b) tested levy against `full_solvency`, which demands the Trust's whole 630M dividend be replaced from levy EVERY period — the unloaded baseline misses it by 1.3–9.4× at every ε (a property of the dividend design). Correct target is `cover_expenditures`. **The gate now re-runs a null load every time and reports `baseline_passes`/`attributable`.** Any future gate must do this.
WIRING: `thermal_obligation` is a 4th term in ecological_eoh (total = baseline + spike + visible_deferred + thermal), plumbed through breakdown → ecological_eoh → total_eoh → eoh_to_teh_pipeline → ecological_allocation → fiscal_snapshot, default 0.0 everywhere (opt-in, no prior caller moves). NOT scaled by monitoring — measured forcing is directly observed, unlike historical neglect. Partly self-funding via the ledger identity (modelled, not assumed).
RESPONSIBILITY DATA GAP (open): shipped OWID energy has fossil only from 1965, GHG only from 2000. A truncated series UNDER-charges early industrializers — exactly who the rule exists to charge. `allocation_share()` takes cumulative emissions explicitly and falls back to population WITH a declared caveat. **Needs owid-co2-data.csv (`cumulative_co2` back to 1750) dropped into handoffs/thermal_sink/data/.** Demo with the truncated proxy: US 5.6× headcount, Singapore 7.0×, Germany 3.8×, India 0.23×, Bangladesh 0.09× — ~65× spread.
ERA5 / P2 UNBLOCKED — η may be computed from OLR DIRECTLY. Diurnal aliasing bound MEASURED on three years by joining the 20-hour validation file (hours 1–5,7–11,13–17,19–23 — the exact COMPLEMENT of the synoptic hours, deliberate) with the four synoptic files:
  worst zonal (continent-correlated) bias, W/m²:  1940 0.53 | 1980 0.20 | 2020 0.41  (4-point)
                                                  1940 4.86 | 1980 3.45 | 2020 4.69  (12-UTC only)
  → 9–17× reduction; residual <0.2% of mean OLR; per-cell RMS 5.6–5.9 vs 17.9–18.5. STABLE across 80 yr, no trend, diurnal amplitude flat-to-declining (54.6→52.9). Validation: 24-h global mean OLR 241.97 W/m² (textbook ~240); ttr/3600 reproduces avg_tnlwrf to 6.3e-06.
ERA5 TRAPS (both cost a re-run; now in utils/grib_scan.py docstring + tests): (1) files carry BOTH 0.25° reanalysis (Ni=1440) AND 0.5° ensemble (Ni=720) under the same shortName at 3-hourly steps — keying by hour alone lets the ensemble silently overwrite; **filter Ni==1440**; (2) REFERENCE time ≠ VALIDITY time — accumulated fields valid 00–06Z on the 1st carry the PREVIOUS month's 18Z reference, so a month filter drops 6 h of radiation silently; data12utc's "06Z" stamps are step-6 fields valid at 12Z.
TOOL: `utils/grib_scan.py` (committed d7217fe) — header-only GRIB walk, stdlib only, seconds vs pygrib timeouts on 200 GB. `timestamps()` and `find(path,y,m,d)`; find() returns the accumulation-base block by default. Locate-then-decode turned a 200 GB scan into a 1.85 GB read. Validated vs eccodes 120/120. Also learned: month sampling is Jan/Mar/May/Aug/Oct/Dec — NOT evenly spaced (+2,+2,**+3**,+2,+2); year stride 5 yr confirmed. rawdata/ = 813 GB, 133 GB free.
NEXT: P2 η extraction proper (clear-sky basis + all-sky alongside, lsm mask, normalise to global land mean, ship reference/data/eta_land.json + examples/ script — never the GRIB). Handoff §7.2 carries the measured bound (handoffs/ is gitignored).
ALLOCATION DOCTRINE (2026-08-05, author, commits 3ef9ae1 + ad8e177) — THE FRAMING TO REUSE, not just for thermal: **the framework is built to work going forward, not to be a complete record of the past.** Exhaustive backward accounting is impossible AND self-defeating — records are biased toward whoever kept documentation, so the more history an allocation demands, the more it privileges the well-documented. A line must be drawn or there is no end to how far back one goes. **Looking back sets a STARTING POINT, not a verdict.** Pick a defensible line, allocate what is known, move forward — that begins solving and preventing, which a perfect ledger of blame never does. Doctrine text ships INSIDE reference/data/cumulative_emissions.json (`_doctrine`) with a test asserting it travels with the numbers.
Two rulings from it: (1) unattributed emissions (international shipping 29.6 Gt + aviation 16.3 Gt = 46 Gt, 2.49%, belong to no territory) → **redistribute PRO-RATA** (`CDR_UNATTRIBUTED_POLICY="pro_rata"`, normalises shares to 1) — "we all inherited the world as is"; superseded by consumption-based once trade data supports it, and OWID's starts 1990 so 1990-forward is where we start rather than waiting. (2) **land use STAYS IN** (`CDR_RESPONSIBILITY_BASIS="incl_luc"`) — land converted inside a collective is that collective's whatever the demand that motivated it; the external-demand argument is REAL but recorded for live implementations, not resolved by the model.
RESPONSIBILITY RESOLVED with owid-co2-data.csv (user supplied 2026-08-05, in handoffs/thermal_sink/data/, gitignored). Shipped `reference/data/cumulative_emissions.json` — 215 collectives, 1750–2024, both bases + population share + provenance + doctrine. `responsibility_share(collective, basis, unattributed)` and `allocation_share(..., collective=...)` in research/thermal_drawdown.py; unknown name → None (NOT zero — a zero share would silently excuse a collective) → allocation_share falls back to population WITH declared caveat. TRUNCATION BIAS MEASURED (vindicates the earlier flag): vs a 1965+ fossil-energy proxy the UK was under-charged **1.70×**, Germany 1.31×, Russia 1.16×, while Singapore was OVER-charged 4.1× and Qatar 1.70× — truncation flatters early industrializers and penalises late ones. Never use a truncated cumulative basis.
BOOKED OBLIGATION per collective (ΔT 2.0 K, 40 yr, 0.6 h/t, pro-rata incl_luc): US 2,980 M EOH/yr = 4.82× headcount = 8.62 h/person/yr; Russia 4.65×; UK 3.42×; Germany 3.33×; China 0.75×; India 0.22×; Bangladesh 0.06× = 0.11 h/person/yr. **~80× per-person spread US↔Bangladesh** — the equity commitment made numerical. Gate still passes at every ε.
Branch research/measurement-spine now 8 commits ahead of main; 2031 tests, mypy clean, tree clean.
P2 η SHIPPED (2026-08-05, commit 581cec3). `reference/data/eta_land.json` — 258 collectives, ERA5 1940–2025, clear-sky + all-sky; 207 direct, 48 neighbourhood-inherited (radius recorded), 3 NULL (Kiribati/Marshall Is./Clipperton — no land fraction within 5°, below ERA5 resolution; return None, never a fabricated value). `eta_for()` + F11 wiring: A_eff = η·a, applied BY DEFAULT when the name resolves — this CHANGED existing F11 numbers by design (Singapore 84.2→79.2); un-weighted recoverable via eta=1.0 and the test pins both plus their ratio. **η moves U by −9% to +18% and NO collective crosses Contact — real but second-order, not a rescue.** Germany/UK go slightly DEEPER into Contact.
η METHOD DECISIONS (reusable): clear-sky basis (all-sky rewards weather; cloud isn't a policy lever) with all-sky shipped alongside as reality check — the GAP is the diagnostic (Singapore +0.129, Brazil +0.066, Gulf −0.05..−0.08). Weight by land FRACTION not binary mask. **Normalise over CLAIMED land, Antarctica EXCLUDED** — matching A_LAND_CLAIMED_M2; normalising over all land inflates everyone 2.96% and since η redistributes a FIXED budget that silently changes the total allocated (caught pre-ship). Only diurnally balanced days (107/108). Validation: land cloud radiative effect 21.2 W/m² vs published ~20, from two independent fields.
MARGINAL CAPACITY = RECORDED NEGATIVE RESULT (do not repeat). η is TOTAL OLR so hot arid land scores high; the objection that this may be backwards STANDS. dOLR/dT built 3 ways, data can't support it: seasonal 35.2% of land r²<0.5; diurnal BEST conditioned (5.8%) but WRONG timescale (column fixed, whereas waste heat is sustained); interannual RIGHT timescale but WORST (57.6%, UAE r²=0.02). Brazil spans 0.802–1.612 across them. Resolves_by 12-month sampling = DATA requirement, no rearrangement of the present archive fixes it. Physical check: mean dOLR/dT 1.4–1.9 vs graybody 4σT³ 5.42 — the water-vapour effect visible.
ERA5 2015 GAP CLOSED: `data201512utc.grib` (user fetched, 11.1 GB, full catalogue) — the 12 UTC archive had been missing ALL of 2015, so that year had no noon. 107 balanced days, 18/18 years.
UNCOMMITTED, held open by author decision: λ derivation + ε_current H ratio. λ_historical = (F−N)/ΔT = **1.492 Tier A**, four windows 1995–2024 within 5% (1.466–1.537), band 0.52–2.44 dominated by AEROSOL forcing uncertainty. Pattern effect +0.182 (right sign/scale = independent check). Shipped 1.2 implies ECS 3.28 K and is BELOW both AR6-implied 1.310 and historical 1.492 — i.e. CONSERVATIVE (lower λ → smaller budget → larger obligation); the old constant was not flattering us. FRAME DISCIPLINE ENFORCED: `budget_forcing_headroom` RAISES on historical-magnitude λ under frame='equilibrium' (inflates allowance ~6×). **NEW FINDING: at AR6's likely-high sensitivity (ECS 5 K, λ=0.786) the budget at ΔT_max 3.0 K is ZERO** — λ uncertainty ALONE can close the budget, a second route to "unbudgeted" independent of the forcing-band determinacy map. Budget spans 0 → ~11× across AR6's likely ECS range (wider than the 6.5× previously estimated, which compared point values). ε_current: `global_ceiling` now leads with **H = budget/Φ = 5.39 (measured, no chosen constant)**; ε_max = H·ε_current is a labelled conversion; test asserts H invariant to ε_current.
NEXT DIRECTION (author): lead with `determinacy_zone()` — the claim that needs NO ΔT_max — and treat ΔT_max assessment as the real research task (assess in land TXx, ÷1.48, naming what actually binds: wet-bulb survivability / crop failure / ecosystem collapse). λ has gone as far as data allows; what remains is an assessed equilibrium feedback with uncertainty.
THERMAL ARC CLOSED (2026-08-05, commit a28c629 — λ derivation + two-axis determinacy + verdict). Branch research/measurement-spine, 10 commits ahead of main, 2078 tests, mypy clean, tree clean.
**LEAD WITH DETERMINACY** is now the layer's structure: `thermal_lambda.thermal_verdict()` reports the ZONE first and WITHHOLDS the budget in the indeterminate band (budget_tw=None, not estimated) — a number from inside a band containing both "no budget" and "ample budget" is a point estimate masquerading as a result. Where a budget IS reported it's the WORST CORNER (lower bound). **At ΔT_lo = 3.0 K — the value every headline in this layer was quoted at — the framework now declines to state a budget.** CLI `eoh thermal determinacy` defaults to two-axis; `--single-axis` for the superseded map.
TWO-AXIS MAP: budget exists iff λ·ΔT > F; determinacy needs the WHOLE box to agree, so carrying λ can only WIDEN the indeterminate band — an extra uncertain axis NEVER buys agreement (I initially predicted the opposite; corrected). single-axis unbudgeted-below 2.168 K GMST / 3.21 K TXx → two-axis likely 1.655 K / **2.45 K TXx** → very-likely 1.324 K / 1.96 K TXx. **Observed land TXx ≈1.8 K, so the margin to the strongest determinate claim is ~0.65 K (likely) / ~0.16 K (very likely).** §4.2's published "assess under 3.21 K TXx → determinately zero" was resting on a fixed unassessed λ and DOES NOT SURVIVE. `determinacy_zone()` marked SUPERSEDED, retained for audit.
MY ERROR, caught while pursuing the strongest position: I shipped ECS [2,5] — AR6's VERY LIKELY (90%) — labelled "likely". Actual likely (66%) is ECS 2.5–4.0 → λ [0.983, 1.572]. Carrying the 90% range under a 66% label overstated uncertainty and understated our own claim. Both ranges now ship correctly labelled; likely is default; a test pins the labelling. LESSON: check confidence-interval labels before carrying a band.
λ DERIVED: λ_hist = (F−N)/ΔT = **1.492 Tier A**, four windows 1995–2024 within 5%, band 0.52–2.44 dominated by AEROSOL uncertainty. Pattern effect +0.182 (independent check). Shipped 1.2 = ECS 3.28 K, BELOW AR6 best 1.310 and below historical → CONSERVATIVE, was not flattering us. FRAME ENFORCED: `budget_forcing_headroom` RAISES on historical-magnitude λ under frame='equilibrium' (~6× inflation). Budget spans ZERO (ECS 5 K) → ~11× across AR6 range: **λ uncertainty ALONE can close the budget**, a second route to unbudgeted independent of the forcing band.
ε_current RESOLVED: `global_ceiling` leads with **H = budget/Φ = 5.39** (measured, no chosen constant); ε_max = H·ε_current is a labelled conversion; test asserts H invariant to ε_current.
NEXT / OPEN (thermal): **assess λ — now outranks ΔT_max.** λ contributes ~1× the forcing width on the likely range and ~2× on very-likely; a tight [1.10, 1.45] recovers ~1.9 K of determinate zone. It's an ASSESSMENT task not data collection. Assessing ΔT_max says WHERE you sit on the map; assessing λ decides whether sitting anywhere means anything determinate. Also open: marginal-capacity η (negative result recorded, needs 12-month sampling), 3 null atolls, F8/F9 drawdown path (multi-period), `capital_for_epsilon` ε-inverse never built, B3 maintain-vs-replace never built.
λ CANNOT BE ASSESSED HERE — settled 2026-08-05 (commit 77b60ed). Tried a SECOND independent estimator: OLS regression of (F−N) on ΔT, 53 yr 1971–2020 → **1.693 ± 0.472 (95%), r=0.702**, vs ratio-of-means 1.492. Consistent, BUT the band alone spans 1.22–2.17 (±32%) — even the HISTORICAL feedback isn't tightly pinned by these series. And both estimate HISTORICAL; reaching EQUILIBRIUM λ needs the PATTERN EFFECT, which requires pattern-forced model experiments or paleoclimate constraints — NOT derivable from ERF+EEI+GMST. **1.2 stands as a labelled BEST GUESS**: position known (below AR6 best 1.310 and both estimates), direction of error known (conservative — lower λ → smaller budget → LARGER obligation), sensitivity band ships with every derived figure. resolves_by = EXTERNAL assessed ECS, not a rearrangement of what we hold. Recorded in climate_feedback.json `_can_it_be_assessed_here`. DO NOT retry with this archive.
ε INVERSE BUILT (B4 from the old plan, finally — `research/epsilon_inverse.py`, commit 77b60ed). `capital_for_epsilon(target)` bisects the capital SCALE over a fixed MIX so a sweep's coordinate is an ECONOMY and ε stays derived by `core.civilization.civilization_epsilon`. Round-trip asserted (feed capital back → get target ε). Resolves the standing contradiction: CLAUDE.md says ε is an observable "not a policy lever", yet every sweep set it by hand. Reference mix pop 1e6: ε 0.40 ≈ 6,300 TEH/capita, ε 0.99 ≈ 18,500. Needs a SOLVER not a formula because capital appears in BOTH terms of ε = machine_EOH/total_EOH (more capital also generates more infra EOH) — monotonicity + saturation-at-1 are pinned in tests since bisection depends on them and neither is obvious.
**NON-UNIQUENESS QUANTIFIED**: at ε=0.40 the three shipped mixes span **1.30×** in total capital; infrastructure-heavy needs the MOST (most maintenance obligation per unit EOH eliminated). `mix_spread()` measures it. Matters most for thermal — **Φ is a property of the INVENTORY, not of ε** — so two collectives at the same ε with different mixes have different dissipation and different thermal utilization. This is the divergence CLAUDE.md warns ε-as-input conceals. Graduates to core/trajectory.py when the mix parameterisation settles; `canonical_physical_state(ε)` remains the shipped arc reference until then.
STATE: branch research/measurement-spine, 11 commits ahead of main, 2091 tests, mypy clean, tree clean. REMAINING GAPS (none blocking): B3 maintain-vs-replace with the embodied-energy pulse (needs the ε-inverse, now available); F8/F9 drawdown path (needs multi-period engine); marginal-capacity η (negative result — needs 12-month ERA5 sampling); 3 null atolls (Kiribati/Marshall/Clipperton) need a coastal-proximity rule or explicit class handling; ΔT_max assessment (the framework's own judgment, dominates everything).
B3 CLOSED — maintain vs replace (2026-08-05, commit a1d1418). `research/thermal_capital.maintain_vs_replace()` + `replacement_exchange_curve()`. **The first thing in the framework that PRICES ONE DOMAIN'S OBLIGATION AGAINST ANOTHER'S**: labour-hours saved per TJ of extra dissipation. Infra EOH rises with age (age_factor→age_factor_max) so replacing cuts labour; embodied energy is spent AT CONSTRUCTION so each replacement is a PULSE not an amortized flow. Quarterly stepping over the horizon.
RESULT (transportation, 30 yr life, 40 yr horizon): exchange rate rises monotonically with age — 58.7 EOH/TJ at age 1.5 → 170.4 at age 24, **2.9× better**. RULE: **replace at END OF LIFE, not before** — early replacement isn't just capital-expensive, it's thermally worse, and the penalty is now priced. This is F9 ("crash transitions are thermally worse") at ASSET scale, from the same arithmetic.
NULL ASSERTED: over a horizon that's a whole number of design lives both strategies do identical total aging AND identical replacement counts → difference is exactly zero. My first test case (age 20, life 30, horizon 60) sat on that degeneracy and looked like a bug; it's real. Test pins it so the model can't invent a gap.
CAVEAT carried in the RETURN VALUE (not just docstring): condition proxies UTILISATION in the shipped model, so an aged asset draws LESS (does less work) rather than costing more per unit output — no efficiency decay. Biases AGAINST replacement → the replace case is a CONSERVATIVE FLOOR. ε-behavior: labour saved scales (1−ε), dissipation doesn't, so the exchange rate steepens with automation.
STATE: branch research/measurement-spine, **12 commits ahead of main**, 2099 tests, mypy clean, tree clean. Remaining gaps (none blocking): F8/F9 drawdown PATH (needs multi-period engine); marginal-capacity η (negative result — needs 12-month ERA5); 3 null atolls need a coastal-proximity rule or explicit class; ΔT_max assessment (framework's own judgment, dominates all); typed-capital integration into core/capital.py; supply-curve calibration.


<a id="capital-thermal-dual-output"></a>

**CAPITAL THERMAL DUAL-OUTPUT — ONE INVENTORY, TWO OUTPUTS** (2026-08-01, committed `d13613e` 2026-08-04, promoted from session memory 2026-09-03). Verbatim.

---
name: project-capital-thermal-dual-output
description: §12.2 capital adaptation built — same capital inventory yields both ε (EOH) and Φ (thermal); closes the capital→thermal-ceiling loop
metadata: 
  node_type: memory
  type: project
  originSessionId: 2437c2f6-8846-4740-9dc4-00f2c310126b
---

The capital adaptation that was PAUSED pending the thermal spec (§12.2) — built 2026-08-01, committed 2026-08-04 in d13613e. Closes the loop the thermal vector required: the SAME capital inventory that produces ε now also produces the collective's thermal load Φ, so the corridor's thermal ceiling can be computed bottom-up from the framework's own capital model rather than only top-down from Path C national energy stats.

BUILT:
- `data.py`: `CAPITAL_THERMAL_PROFILES` — parallel dict (all 11 CAPITAL_MACHINE_PROFILES types), the §12.2 fields: `power_intensity_w_per_teh` (net operational draw) + `embodied_energy_j_per_teh`. Kept SEPARATE from CAPITAL_MACHINE_PROFILES (they're CHOSEN placeholders, research-tier) — avoids a risky 11-entry edit and visually separates established EOH model from experimental thermal overlay. design_life (existing) is the 3rd §12.2 field; grid κ is a collective input (§8.1), not per-type → `THERMAL_GRID_KAPPA_DEFAULT`=0.93. All CHOSEN with epistemic pointers (IEA end-use intensity; ecoinvent/EPD LCA).
- `research/thermal_capital.py`: `machine_dissipation_from_capital` (Φ = Σ teh·cond·power_intensity + teh·embodied/(design_life·Δt_s), ×κ̄; DRY — reuses machine_eoh_from_capital's by_type), `collective_thermal_from_capital` (→ ψ, U, regime), `capital_thermal_ceiling` (→ measured_thermal_ceiling → corridor). `tests/test_thermal_capital.py` (15). Suite 1921 pass, mypy clean (54 files).

HONEST RESULTS (not tuned): well-invested collective (all-standard, 1902 TEH/person) → ONE inventory yields EOH (266M h/yr) AND Φ (3.19 GW = 2.99 op + 0.20 embodied). Aggregate 3187 W/person vs Path C measured ~2200 (within ~1.5×, reasonably higher for a fully-built collective — order-consistent, NOT fitted). Heavy dissipators rank as physics expects: computing_ai > power_grid > industrial_automation > transportation. Dense collective (730 km²) → U=1.64 Contact → corridor CLOSES from its own capital; large (9.15M km²) → below floor → open. Path-B-shaped STRUCTURE on Path-D magnitudes: deliverable is the closed loop (capital → {ε, Φ, U, ceiling}), not the numbers.

COMMITTED 2026-08-04 in d13613e on research/measurement-spine (whole thermal+corridor+capital arc). See [[project-thermal-sink-vector]] [[project-infrastructure-eoh-floor]] [[project-multiplier-v5-adoption]]. Open: intensities need Path-B measurement (IEA/LCA) before magnitudes are load-bearing; typed-Asset (core/capital.py) routing still not done; ε-vector Mission Statement amendment still sign-off-gated; whole staged bundle needs a branch decision (user wanted capital on a new branch after multiplier work).

<a id="finding-a-capital-profile-scale"></a>

**FINDING A — CAPITAL-PROFILE SCALE IS UNCHECKED AGAINST ANY REAL INVENTORY** (2026-08-08, promoted from session memory 2026-09-03; the ledger filed it as *"belongs to thermal/corridor"* and it sat unpromoted for four weeks). Verbatim.


Running the US through `epsilon_current_from_inventory()`: BEA NIPA **Table 5.2.5 is a FLOW table** (investment + CFC per year), so a stock needs `K = CFC/δ` on 2024 CFC $4,796.7B. USD/TEH derived from the registry itself — mean wage **$66,213** ÷ 2,080 h ÷ mean multiplier **1.9964** = **$15.95/TEH** ($16.58 at 2,000 h; ~$21 with benefits, since wages ≠ compensation; user's 17.5 is sound and conservative). Result: K = $64–107T → **9,091–18,182 TEH/capita → ε = 0.777–1.000, SATURATED**. Against `capital_for_epsilon(0.40)` = **4,204 TEH/capita**: the US is **2.2–4.7× over**. A US at ε≈0.95 is not credible (ε=0.99 is "prices collapsed, human labour near-zero"; 158M people work), and Block II's 84.4%-care residual points the same way. **So the profile `teh_per_capita` tier scale or the `personal_fulfillment_rate` values are miscalibrated by ~3×** — and the capital→ε loop closed by the 2026-08-05 audit, plus the thermal ceiling and corridor, all ride on it. `mix_spread(0.40)` = only 1.30×, so the MIX is not the problem; the SCALE is. **Unblocked by BEA Fixed Assets Table 1.1 (stock) + 2.1/3.1ES (by asset type)** — same agency, one table over from `handoffs/us_inventory/`; gives the inventory but does NOT by itself resolve the 3×.
